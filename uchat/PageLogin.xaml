<Page x:Class="uchat.PageLogin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:UserTextBox="clr-namespace:uchat">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="130"/>
            <RowDefinition Height="130"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="uchat" FontSize="40" FontWeight="Bold" VerticalAlignment="Center" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <UserTextBox:InputTextBox x:Name="NameTextBox" Grid.Row="1" HorizontalAlignment="Center" 
                                  Header="User" Placeholder="Enter username" />
        <UserTextBox:InputTextBox x:Name="PasswordTextBox" Grid.Row="2" HorizontalAlignment="Center" 
                          Header="Password" Placeholder="Enter password" IsPassword="True"/>

        <Button Grid.Row="3" Width="300" Height="70" Content="Log In" FontSize="30" Foreground="White" FontWeight="Bold" 
                Cursor="Hand" Click="LogInButton_Click" Background="#7658B2" BorderBrush="#604184" BorderThickness="5">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border CornerRadius="15"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <TextBlock Grid.Row="4" HorizontalAlignment="Center" Margin="0,10,0,0" FontSize="16">
            <Run Text="Don't have an account? "/>
            <Run Text="Register" FontWeight="Bold" TextDecorations="Underline" Cursor="Hand" MouseDown="RegisterText_MouseDown"/>
        </TextBlock>

    </Grid>
</Page>